// src/components/dashboard/AddonsPage.tsx
// Export the simple version that doesn't cause logout issues
export { default } from './AddonsPageSimple';

// The original complex version is kept as AddonsPageComplex if needed
// export { default } from './AddonsPageComplex';